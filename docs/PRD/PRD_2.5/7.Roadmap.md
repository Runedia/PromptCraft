# §7 Roadmap

PromptCraft의 성공 지표(KPI)와 향후 과제를 정의합니다. 시나리오2 평가(R1·R2·R3 × L1·L2·L3 × A·B = 18단위, [§6 Evaluation](./6.Evaluation.md) 참조)에서 도출된 실측 데이터와 디자인-구현 격차([§4 UI-Design](./4.UI-Design.md) 참조)를 반영하여 KPI를 정의하고 과제 우선순위를 배치합니다.

KPI는 시나리오2 결과로 일부 확정되며, 미측정 항목은 P0 측정 인프라 과제로 명시적으로 이관합니다. 향후 과제는 P0(다음 사이클 전제) / P1(핵심 기능 개선) / P2(후속 평가 사이클)의 3단계 우선순위로 분류합니다.

---

## 7.1 성공 지표 (KPI)

### 7.1.1 자동 검증 / 벤치마크 KPI

14개 지표 중 자동 테스트로 검증 가능한 PromptCraft 자체 동작 항목과 A/B 벤치마크가 필요한 항목을 정리합니다. 시나리오2 평가에서 실측이 확보된 항목에는 ✅ 마커를 표기하고, 측정 불가 항목은 P0 과제와 연결합니다.

| # | 지표 | 측정 방법 | 시나리오2 실측 | 비고 |
|---|---|---|---|---|
| 1 | One-Shot 응답 성공률 | A/B 벤치마크, 빌드 통과 + 핵심 목표 충족 | ✅ B군 100% (L3 18/18 PASS), A군 100% (G1 한정) | 시나리오 평가에서 측정 |
| 2 | 학습 곡선 — 카드 사용률 감소 | 종단 연구 (세션 누적) | (TBD) | 단발 측정 불가 → 7.2.3 P2 |
| 3 | 섹션 제어 정확성 | active 카드만 프롬프트에 포함되는지 단위 테스트 | ✅ 자동 테스트 PASS | — |
| 4 | 빈 헤더 출력 zero | value가 비어있는 active 카드 미출력 자동 검증 | ✅ 자동 테스트 PASS | — |
| 5 | 프리뷰 반응속도 | 카드 입력 → DOM 갱신 100ms 이내 | ✅ [§3 Features](./3.Features.md) 기준 충족 | — |
| 6 | LLM 호출 zero | 코어 플로우 내 외부 API 호출 부재 검증 | ✅ 자동 테스트 PASS | — |
| 7 | Run as 보안 | Provider 화이트리스트 우회 시도 차단, 셸 경유 실행 부재 | ✅ 통합 테스트 PASS | — |
| 8 | 구동 안정성 | TypeScript 컴파일 에러 Zero, 빌드 단계 에러 Zero | ✅ CI PASS | — |
| 9 | 테스트 커버리지 | `bun test --coverage` 라인 커버리지 전역 90% 이상 | ✅ 기준 충족 | — |
| 10 | 보안 검증 | 경로 순회 시도 시 403 응답, CSP 위반 zero | ✅ 통합 테스트 PASS | — |
| 11 | @멘션 보안 | pathGuard를 통한 프로젝트 루트 외 파일 접근 차단 100% | ✅ 단위 테스트 PASS | — |
| 12 | DB 마이그레이션 | 기동 시 자동 적용 성공, 실패 시 백업 후 중단 동작 검증 | ✅ 통합 테스트 PASS | — |
**해석.** 후반부 10개 지표(#3~#12)는 자동 테스트로 검증 가능한 PromptCraft 자체 동작 항목이며 시나리오2 평가에서 회귀 없이 통과했습니다. 전반부 2개 지표 중 #1(One-Shot)은 부분 확정되었고 #2(학습 곡선)는 종단 연구로 별도 분리됩니다.

### 7.1.2 평가 도출 KPI

시나리오2 평가에서 PromptCraft 효과가 가장 결정적으로 입증된 영역과 모델 신뢰성 격차가 드러난 영역을 정식 KPI로 등재합니다. 정의·측정 방법·목표의 3컬럼 형식을 채택합니다.

| # | KPI | 정의 | 측정 방법 | 목표 |
|---|---|---|---|---|
| 13 | 항목 누락 감소율 | 시나리오 입력의 모든 항목이 응답·구현에 포함된 비율. A군 대비 B군의 응답 항목 누락 비율 감소도. | 시나리오 입력의 항목 목록을 사전 정의 → 응답·git diff와 1:1 매칭 → 충족(✅) / 부분(⚠️) / 미충족(❌) 카운트 | A군 대비 누락 ≥30% 감소, B군 충족률 ≥95% |
| 14 | 응답-구현 정합성 | 응답 텍스트의 주장(라인 수·파일명·변경 항목·scope)과 실제 git diff의 일치도. 과장·축소 사례 0건. | LLM judge 자동 측정 또는 정적 분석(응답에서 수치·파일명 추출 후 git diff 실측과 ±1 오차 허용 비교) | ≥ 90% (자동 검증 도입 후 100%) |
| 15 | Constraints 위반율 | L3-B 시나리오 Constraints 카드의 모든 제약(외부 crate 추가, 시그니처 보존, scope 한정) 준수율. | git diff에서 Cargo.toml dependencies / 함수 시그니처 / 파일 scope 변경 정적 검출 + grep 자동화 | B군 위반율 < 5% (시나리오2 실측 0%) |
| 16 | 빌드 통과율 (G1) | 적용된 변경이 빌드 도구(`cargo check`, `npm build` 등)를 통과하는 비율. | CI 자동화 (exit code + warning 카운트) | 100% (하한 기준) |

**KPI #13 (항목 누락 감소율) 근거.** 시나리오2 2차 평가에서 H7 가설("L3-B 항목 누락 감소")이 가장 강하게 입증되었습니다. 1차 응답 텍스트 도메인에서 A군 평균 92%, B군 평균 99%이며 B군 분산도 더 작았습니다. 2차 코드 적용 도메인에서도 L3-B가 핵심 3목표를 R1·R2·R3 모두 3/3 충족했습니다. 이는 PromptCraft의 핵심 가치 제안이므로 정식 KPI로 등재합니다.

**KPI #14 (응답-구현 정합성) 신설 근거.** 시나리오2 2차 평가에서 R2 L2-B의 "타입 안전성" 단언 과장, R2 L2-B "170줄" 수치 과장, R3 L1-A의 92% 항목 누락 보고, R3 L2-A의 timeout 회귀 침묵 등 응답 텍스트와 실제 git diff의 불일치가 모델별로 검출되었습니다. 모델 간 신뢰성 격차가 가장 크게 드러나는 지표이며 자동화 가능성이 높습니다.

**KPI #15 (Constraints 위반율) 근거.** 시나리오2 2차 평가에서 H3 가설("L3-B Constraints가 외부 crate 추가를 차단")이 완전 입증되었습니다. A군 11.1% (2/18) 위반, B군 0% (0/18) 위반이며 R1·R2·R3 모두 B군 0%가 일관 관찰되었습니다. PromptCraft의 가장 결정적인 효과 지표이므로 정식 KPI로 등재하며, 후속 평가 사이클의 비결정성을 고려해 목표는 < 5%로 완화합니다.

**KPI #16 (빌드 통과율 G1) 근거.** 시나리오2 2차 평가에서 18/18 PASS (100%)가 실측되었습니다. A·B군 모두 100%로 본 라운드에서 PromptCraft의 차별 효과는 관찰되지 않았으나, 향후 라운드에서 A군 빌드 실패 가능성(특히 광역 scope creep 케이스)을 고려하여 하한 기준 KPI로 등재합니다.

### 7.1.3 KPI 종합표

| 구분 | 지표 수 | 측정 상태 |
|---|---|---|
| 벤치마크 (#1~#2) | 2개 | #1 부분 확정, #2는 종단 연구 (§7.2.3) |
| 자동 검증 (#3~#12) | 10개 | ✅ 전량 PASS |
| 평가 도출 (#13~#16) | 4개 | #13·#15·#16 실측 확보, #14는 P1 자동화 의존 |
| **총계** | **16개** | — |

---

## 7.2 향후 과제 (단계별)

시나리오2 평가에서 도출된 과제와 디자인-구현 격차에서 결정 필요 항목을 통합합니다. P0/P1/P2 3단계 우선순위로 분류합니다.

### 7.2.1 P0 — 평가 운영 (다음 사이클 전제)

다음 평가 사이클을 의미 있게 수행하기 위한 최소 운영 메타데이터입니다. 토큰 사용량·latency 추적은 외부 도구(예: `getagentseal/codeburn`)에 위임하고, PromptCraft에서는 평가 식별성 확보에 한정합니다.

| # | 과제 | 설명 | 연결 KPI |
|---|---|---|---|
| P0-1 | **모델 식별 메타데이터** | `테스트 구성정보.txt`에 `model_id`(예: `claude-opus-4-7@2026-05-22`), 세션 ID를 명시. R3 비공개 평가에서 누락되었던 항목. 후속 평가의 모델별 약점 패턴(R2 단언 과장, R3 scope 축소) 추적 가능. | KPI #14 |

### 7.2.2 P1 — 핵심 기능 개선

PromptCraft 자체 기능 확장과 디자인 결정 항목입니다.

#### A. 평가 자동화 / 기능 확장

| # | 과제 | 설명 | 비고 |
|---|---|---|---|
| P1-1 | **응답-구현 정합성 자동 검증** | LLM judge 또는 정적 분석으로 R2 L2-B "타입 안전성" 단언, R3 L1-A "92% 누락" 보고 같은 응답-구현 불일치를 자동 검출. KPI #14의 자동화 경로. [§3.3.1](./3.Features.md) 참조. | KPI #14 자동화 |
| P1-2 | **검증/가드레일 단계 자동 삽입** | 빌드 완료 시 "Defensive Thinking" 요구 지시어 선택 삽입. 시나리오2의 응답-구현 불일치 발견으로 우선순위 상향. | — |
| P1-3 | **자동 제약 조건 매핑** | `tsconfig`, `.eslintrc`, `Cargo.toml` 등에서 제약 자동 추출 → `constraints` 카드 자동 채움. KPI #15 효과를 L3 시나리오 외 영역으로 확장. | — |
| P1-4 | **근거 문서화(Rationale) 자동 삽입** | "이 요청을 하는 이유"를 구조화 섹션으로 자동 추가. 모델이 의도를 더 정확히 파악. | — |

#### B. 디자인 시스템 정합성

[§4 UI-Design](./4.UI-Design.md)에서 식별된 디자인-구현 격차 중 결정 필요 항목입니다.

| # | 과제 | 설명 | 비고 |
|---|---|---|---|
| P1-5 | **폰트 셀프 호스팅 전환** | 현재 Google Fonts CDN 직접 import (`design-system.css:1`)이 [§5.2.1](./5.Architecture.md) 네트워크 격리 원칙과 충돌. `@fontsource/*` 패키지로 전환. | 네트워크 격리 원칙 |
| P1-6 | **누락 토큰 보강** | `--pc-bg-inset`, `--pc-bg-overlay`, `--pc-border-inset`, `--pc-fg-subtle`, `--pc-accent-hover`, `--pc-accent-border`, `--pc-info`, `--pc-radius-xl` 16px, `--pc-shadow-pop` 등 후속 화면 도입 시 필요한 토큰을 `@theme` 블록에 추가. amber accent 매핑 보강. | shadcn 표준 매핑 유지 |
| P1-7 | **다크 모드 prefers-color-scheme 반영** | 현재 `ThemeProvider.tsx`의 `enableSystem={false}` 설정으로 수동 토글만 동작. 시스템 환경 감지 추가. | 디자인 시스템 |
| P1-8 | **다크 accent bg 알파 조정** | 현재 `rgba(251,146,60,0.12)` (`design-system.css:98`). 시각 대비 검토 후 조정. | 디자인 시스템 |

#### C. UI 변형 / 단축키 확장

| # | 과제 | 설명 | 비고 |
|---|---|---|---|
| P1-9 | **Dual-Pane 워크스페이스 옵션 추가** | 현재 Three-Column 채택(`c705d25`). Settings에서 Dual-Pane 등 대안 레이아웃 선택 가능하도록 확장. | 사용자 선택형 옵션 |
| P1-10 | **⌘K 커맨드 팔레트** | 파워 유저 모드로 도입. §3.2.11 키보드 단축키 명세 확장. | 신규 도입 |
| P1-11 | **History UI** | `WORK_PREVIEW_HISTORY_BTN` 식별자만 정의(`ui-ids.ts:304`)되고 렌더 0건. `/api/runs/:runId/log`, `/api/history` 소비 UI 구현. | API 소비처 부재 |
| P1-12 | **Settings UI** | `/api/config` 소비 UI 부재. 디자인 변형 선택, 폰트 셀프 호스팅 토글, 시스템 테마 연동 등 P1 항목 노출 창구. | 미구현 |
| P1-13 | **Tab / Shift+Tab 카드 포커스 이동** | PRD §3.2.11 명세 미구현. 브라우저 기본 Tab 동작만 존재. | 접근성 |
| P1-14 | **Mention `:line-range` UI hint** | 동작은 구현(`MentionInput.tsx:178-183`)되었으나 UI hint 부재. ":line-range 지원" 캡션 도입. | UI 단서 |
| P1-15 | **empty 카드 editor 측 시각 단서** | 현재 PromptPreview에서만 "(비어 있음 — 출력에서 제외)" 표시. "EMPTY" mono caption(warning 색)을 editor 측에 추가. | 질문 행동 교정 강화 |

### 7.2.3 P2 — 후속 평가 사이클

후속 평가 사이클에서 수행할 정량 분석 항목입니다.

| # | 과제 | 설명 | 의존 |
|---|---|---|---|
| P2-1 | **3회 반복 평가** | 비결정성 분산 정량화. R1 L2-A 재시도에서 S→A 회귀(`test003_2`)가 비결정성 분산을 직접 입증했으며, 각 시나리오 2회 추가 실행으로 등급 변동 폭을 측정. | P0-1 |
| P2-2 | **신규 모델 추가 평가** | 모델 식별 메타데이터 확보 후 모델별 강점 패턴 추적. R2(단언 과장), R3(scope 축소 보고) 같은 모델별 약점 패턴이 일관 분포인지 단발 분산인지 확인. | P0-1 |
| P2-3 | **L1 시나리오 입력 재정의** | L1 자연어 입력의 모호성이 두 방향(R1/R2 회피, R3 scope creep) 일탈을 유발. L1 시나리오 폐기 또는 "최소 정보 시나리오"로 재정의 검토. | — |
| P2-4 | **R3 L1-A 재시도** | scope creep이 모델의 일관 패턴인지 단발 분산인지 확인. 비결정성 검증 차원. | P2-1과 통합 가능 |
| P2-5 | **release 빌드 검증 확장** | 시나리오2는 `cargo check`만 통과. `cargo build --release`, `cargo test`, `--features stealth` 변형 별도 검증 필요(특히 R3 L1-A의 6 crate 광역 변경). | — |
| P2-6 | **학습 곡선 — 카드 사용률 감소** | KPI #2 측정. 종단 연구(세션 누적)로 별도 설계. | — |
| P2-7 | **L1 일탈 자동 분류 도구** | 회피 vs scope creep 자동 라벨링. 시나리오2에서 두 방향 일탈이 검출되어 분류 자동화 필요. | — |
| P2-8 | **i18n 지원 (ko/en)** | 카드 레이블·hint·examples 다국어화. | — |
| P2-9 | **스캔 결과 캐싱** | `.promptcraft/scan-cache.json`, mtime 기반 diff 스캔. | — |
| P2-10 | **AI 다듬기 (로컬 LLM)** | Ollama 등 연동 후처리. 비용 제로 원칙 유지하며 품질 향상. | — |

### 7.2.4 우선순위 매트릭스

| 우선순위 | 과제 수 | 누적 의존 |
|---|---|---|
| P0 (평가 운영) | 1건 | 다음 사이클 전제 |
| P1-A (평가/기능) | 4건 | 독립 |
| P1-B (디자인 시스템) | 4건 | 독립 |
| P1-C (UI 변형/단축키) | 7건 | 독립 |
| P2 (후속 평가) | 10건 | P0, P1-1 |
| **총계** | **26건** | — |

---

## 7.3 PRD 2.6 예상 영역

다음 버전(PRD 2.6)에서 다룰 항목을 예고합니다. 본 절은 비공식 예고이며 PRD 2.6 작업 시 재검토합니다.

| 영역 | 다룰 내용 | 의존 |
|---|---|---|
| 응답-구현 정합성 자동 검증 결과 | P1-1의 LLM judge 또는 정적 분석 도입 결과. KPI #14 목표 90% → 100% 상향 검토. | P1-1 |
| 신규 카드 풀 추가 | 시나리오2에서 발견된 격차(예: scope 명시 부족, Constraints 외 가드레일) 대응 카드. P1-2·P1-3·P1-4 결과 반영. | P1-2, P1-3, P1-4 |
| 사용자 선택형 UI 변형 평가 | P1-9·P1-10로 도입된 V1/V4 옵션의 사용 패턴 추적. 디자인 결정 baseline 갱신. | P1-9, P1-10 |
| 모델별 강점 패턴 카탈로그 | P2-2의 모델 식별 메타데이터 기반 강점/약점 패턴 정량화. 시나리오 권장 모델 매핑. | P0-1, P2-2 |

---

## 7.4 평가 가이드라인 (참조)

후속 평가 사이클은 시나리오2 평가 가이드라인을 baseline으로 사용합니다. 본 절의 KPI 측정 및 P2 과제 수행 시 다음 문서를 참조하십시오.

| 문서 | 용도 |
|---|---|
| `시나리오2/평가가이드라인.md` | 실행 모드(코드 적용 도메인) 평가 절차. R1·R2·R3 × L1·L2·L3 × A·B 매트릭스 구성, 시나리오 작성, 결과 수집 방법. |
| `시나리오2/평가기준표.md` | G1(빌드 통과) ~ G10(코드 품질) 평가 기준 상세. 등급 산정(S/A/B/C/D) 규칙. |
| `시나리오/평가가이드라인.md` | 1차 응답 텍스트 도메인 평가 절차(참고). |
| `시나리오/평가보고서.md` §5 | 1차 H1~H7 가설 검증 표(참고). |
| `시나리오2/평가보고서.md` §3 | 2차 H1~H8 가설 검증 표(baseline). |

**baseline 정합성 원칙.** 후속 평가 사이클은 본 평가의 도메인(응답 텍스트 + 코드 적용), 가설 번호(H1~H8), 평가 기준(G1~G10), 등급 체계(S/A/B/C/D)를 그대로 계승합니다. 변경 시 PRD 2.6에서 명시적으로 재정의합니다.

---

## 7.5 본 절의 한계 및 결정 필요 항목

본 절은 PRD 2.5의 KPI와 향후 과제를 정의하나, 다음 항목은 결정이 유보되어 있습니다.

| 항목 | 유보 사유 | 결정 시점 |
|---|---|---|
| KPI #16 응답-구현 정합성 측정 비용 | LLM judge 도입 시 비용 발생 → PromptCraft의 "비용 제로" 원칙과 부분 충돌. 정적 분석 우선 검토 필요. | P1-2 설계 단계 |
| P1-9 Dual-Pane 옵션의 default 선택 | 현재 Three-Column이 default. 대안 도입 시 default 변형 결정 필요. | P1-9 구현 단계 |
| P2-4 L1 시나리오 재정의 vs 폐기 | L1이 PromptCraft의 직접 타겟이 아니나 "구조화 강제" 부수 효과 영역. 폐기 시 PRD §2 페르소나 범위 축소. | PRD 2.6 |
| 토큰 측정 인프라 구현 방식 | Provider API 응답 헤더 파싱 vs PromptCraft 자체 계측 vs 양자 병행. 비용·복잡도 trade-off. | P0-1 설계 단계 |

본 절의 KPI 갱신과 과제 분류는 시나리오2 18단위 실측을 baseline으로 하되, 비결정성 분산(R1 L2-A 재시도 S→A 회귀)을 고려하여 단일 측정에 과대 의존하지 않습니다. P2-1의 3회 반복 평가로 분산 폭이 정량화되면 KPI 목표값을 재조정합니다.
