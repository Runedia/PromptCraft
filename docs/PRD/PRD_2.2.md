# PRD — PromptCraft 2.2

**AI 바이브 코딩을 위한 로컬 설치형 구조화 프롬프트 생성 도구**

---

## 1. 개요

### 1.1 배경

AI 코딩 도구(Claude, Gemini, ChatGPT 등)를 활용한 '바이브 코딩(Vibe Coding)'의 성공 여부는 사용자가 제공하는 프롬프트의 품질에 직결됩니다. 그러나 대부분의 개발자는 모호하고 불완전한 질문을 입력하여 다음 세 가지 문제를 반복합니다.

- **잦은 재질문**: 의도 파악에 실패한 AI와의 불필요한 핑퐁
- **토큰 낭비**: 재질문 왕복마다 발생하는 컨텍스트 누적 비용
- **엉뚱한 결과물**: 구체성이 부족한 입력으로 인한 저품질 코드 생성

**기존 AI 프롬프트 보조 도구들의 구조적 문제:**

시장에 출시된 대부분의 프롬프트 보조 도구들은 프롬프트 생성 및 보정 자체에 LLM API를 사용합니다. 즉 이중 비용 구조를 가집니다.

```
기존 도구 구조:
  사용자 요청
    → [LLM API 호출 1] 프롬프트 생성/보정  ← 비용 발생
    → 생성된 프롬프트를 AI에 투입
    → [LLM API 호출 2] 실제 작업 수행      ← 비용 발생
    → (One-Shot 실패 시 추가 왕복)           ← 추가 비용 발생
```

PromptCraft는 이 구조를 근본적으로 해결합니다. 프롬프트 생성 단계에서 LLM을 일절 사용하지 않고, 단일 AI 호출에서 One-Shot 성공을 이끌어내는 구조화된 프롬프트를 로컬에서 완성합니다.

```
PromptCraft 구조:
  사용자 요청
    → [로컬 처리] 스캔 + Q&A + 템플릿 조립  ← 비용 제로
    → 구조화된 완성 프롬프트를 AI에 투입
    → [LLM API 호출 1회] 실제 작업 수행      ← 비용 발생 지점 단 1회
```

### 1.2 제품 정의

PromptCraft는 로컬 코드베이스를 스캔·분석하고, 분기형 Q&A를 통해 사용자의 의도를 구조화하여 AI 코딩 도구에 최적화된 완성형 프롬프트를 생성하는 **로컬 설치형 Node.js CLI/웹 기반 도구**입니다.

**핵심 설계 철학: 프롬프트 생성 비용 제로 + 단 한 번의 AI 호출로 완결**

| 구분 | 방식 | 설명 |
|---|---|---|
| 프로젝트 스캔 | 규칙 기반 (로컬) | 파일 시스템 분석, 언어/프레임워크 자동 감지 |
| 분기 Q&A | 규칙 기반 (로컬) | JSON으로 사전 정의된 의도 파악 분기 트리 |
| 프롬프트 조립 | 규칙 기반 (로컬) | 구조화 템플릿에 수집 데이터 자동 결합 |
| 프롬프트 다듬기 | 선택적 AI (옵션) | 필요 시 로컬 LLM을 통한 후처리 |

### 1.3 One-Shot 성공률과 토큰 효율의 인과 관계

PromptCraft의 토큰 최적화는 단순한 입력 압축이 아닙니다. 실제 효과는 다음 인과 구조에서 발생합니다.

```
구조화된 프롬프트 (역할·목표·컨텍스트·제약이 섹션으로 명시 분리)
    ↓
모델이 의도 파악 단계를 생략
    ↓
사고 토큰(Thinking Tokens)이 전량 문제 해결에 집중
    ↓
One-Shot 응답 성공률 상승
    ↓
재질문 왕복 제거
    ↓
총 API 호출 비용 최소화
```

**절감되는 토큰의 실질 비중:**

| 절감 유형 | 상대적 비중 | 설명 |
|---|---|---|
| 입력 압축 (스캔 요약) | 낮음 | 파일 트리 전체 → 핵심 경로 요약 |
| 재질문 왕복 제거 | 높음 | 대화 1회 = 누적 컨텍스트 전체 재전송 |
| 프롬프트 생성 단계 API 제거 | 높음 | 경쟁 도구 대비 1회 API 호출 완전 제거 |

### 1.4 구조화 템플릿의 설계 근거

Anthropic의 Agent Skills(SKILL.md) 포맷 및 Claude Prompting Best Practices에서 도출된 원칙을 프롬프트 템플릿에 내재화합니다. 이 포맷은 Claude Code뿐 아니라 GPT Codex, Gemini 등 주요 AI 코딩 도구에서 크로스모델 표준으로 수렴 중입니다.

**핵심 원칙:**
- 역할·목표·컨텍스트·제약을 명시적으로 섹션 분리
- 불변 정보(스택/환경)를 상단 배치, 가변 정보(현재 문제)를 하단 배치
- 자연어 산문 대신 구조적 헤더로 정보 레이블링
  → 동일 정보를 산문 대비 적은 토큰으로, 더 높은 해석 정확도로 전달

### 1.5 목표

- **프롬프트 생성 단계의 LLM 비용 완전 제거** (로컬 규칙 기반 100% 동작)
- **One-Shot 응답 성공률 극대화** (구조화 템플릿을 통한 모델 사고 효율 향상)
- **재질문 왕복 및 불필요한 토큰 소비 최소화**
- 프로젝트 컨텍스트를 매번 수동으로 설명해야 하는 반복 작업 제거
- 빠르고 가벼운 100% TypeScript / Node.js 생태계 기반 동작 보장

### 1.6 프로젝트 정보

| 항목 | 내용 |
|---|---|
| 팀 구성 | 최대 4인 |
| 개발 환경 | Node.js 24+, pnpm |
| 개발 기간 | 3 ~ 4개월 |
| 주요 언어 | TypeScript (Node.js) |

---

## 2. 사용자 정의

### 2.1 타겟 사용자

**Primary** — AI 코딩 도구를 자주 사용하며 바이브 코딩을 실천하려는 개발자

- AI에게 상황 설명을 매번 타이핑하는 것이 귀찮은 사람
- 질문을 구체화하지 못해 AI와 불필요한 스무고개를 자주 하는 사람
- LLM API 비용 또는 토큰 소모량에 민감한 사람
- 프롬프트 보조 도구가 또 다른 API 비용을 발생시키는 구조에 불만을 가진 사람

### 2.2 핵심 사용자 시나리오

> 김개발은 진행 중인 Next.js 프로젝트에서 알 수 없는 렌더링 에러를 만났다. 터미널에서 `promptcraft build`를 실행하면 인터랙티브 UI 기반으로 프로젝트 구조를 자동으로 스캔하고, 분기형 질문을 통해 에러 메시지와 원하는 동작을 수집한다. 완성된 구조화 프롬프트가 클립보드에 복사되고, 이를 AI에 붙여넣어 단 한 번에 정확한 해결 코드를 받는다.

---

## 3. 기능 요구사항

### 3.1 기능 목록 및 우선순위

| 기능 | 설명 | 구현 방식 | 우선순위 |
|---|---|---|---|
| 멀티 파서 스캔 | 프로젝트 로컬 디렉토리 고속 분석 (언어/환경 감지) | Node.js (fs) — 로컬 전용 | P0 |
| 분기 Q&A 엔진 | 상황별 분기형 질의 흐름 | Node.js 기반 — 로컬 전용 | P0 |
| 구조화 프롬프트 생성 | 수집 정보를 섹션 분리 템플릿에 조립 + 클립보드 복사 | Handlebars — 로컬 전용 | P0 |
| CLI 인터페이스 (Ink UI) | Ink(React 기반) 터미널 UI 및 명령어 지원 | Node.js CLI — 로컬 전용 | P0 |
| 환경 설정 관리 (Config) | 전역 및 프로젝트 단위 설정 관리 | CLI Config 매니저 | P0 |
| 프롬프트 히스토리 | 히스토리 조회, 상세 보기, 복사, 개별/전체 삭제 기능 | SQLite 기반 CLI | P1 |
| 사용자 정의 템플릿 | 상황별 커스텀 프롬프트 템플릿 로드/저장 옵션 | — | P1 |
| 선택적 AI 다듬기 | 생성된 프롬프트의 자연어 후처리 | 외부 LLM 또는 로컬 LLM (옵션) | P2 |

### 3.2 핵심 기능 상세

#### 3.2.1 프로젝트 로컬 스캔 엔진

`promptcraft scan` 혹은 `promptcraft build --scan` 실행 시 Node.js `fs` 모듈로 지정 디렉토리를 분석합니다. **외부 API 호출 없이 전량 로컬에서 처리됩니다.**

판별 요소:
- 언어 및 프레임워크 (`package.json`, `tsconfig.json` 파일 파싱 등)
- 주요 디렉토리 구조 (`/node_modules`, `/dist` 등 자동 제외)
- 스캔 결과는 **요약본**으로 프롬프트에 주입되어 토큰을 절감합니다.

#### 3.2.2 인터랙티브 분기 Q&A (Ink UI)

Ink 라이브러리(터미널 내 React) 기반의 UI를 통해 사전 정의된 트리 JSON을 렌더링하고 사용자에게 상황에 맞는 질문만 제공합니다.

#### 3.2.3 구조화 프롬프트 빌더

수집된 정보를 **섹션 분리 템플릿**에 조립합니다. 

**섹션 배치 원칙:**
```markdown
## Role
[모델에게 부여할 역할]
## Stack & Environment
[프레임워크, 라이브러리 등 환경 요약]
## Goal
[현재 달성 목표]
## Current Situation
[문제 상황 및 조건]
## Constraints
[제약 조건]
```

#### 3.2.4 CLI 환경 설정 및 추가 옵션

- **Config 명령어 (`promptcraft config`)**: 
  - `list`, `get`, `set` 명령을 통해 설정값 조회 및 변경. 기본적으로 전역 설정(`~/.promptcraft/`)을 다루며, `--project` 플래그로 현재 프로젝트 스코프(로컬) 설정을 오버라이드.
- **History 명령어 (`promptcraft history`)**:
  - `list` (조회), `show` (상세보기), `copy` (클립보드 재복사), `delete` 및 `clear` (삭제) 등 히스토리 라이프사이클 전체 관리 지원.
- **Scan 옵션 확장 (`promptcraft scan`)**:
  - `--json` (JSON 형태 출력), `--save` (로컬 저장), `--depth` (탐색 깊이 직접 지정) 지원.
- **Build 옵션 확장 (`promptcraft build`)**:
  - `--output <file>`: 생성된 프롬프트를 텍스트 파일로 저장.
  - `--template <name>`: 로컬에 저장된 사용자 정의 템플릿 답변 자동 로드.
  - `--scan`, `--no-scan`, `--no-copy` 플래그를 통한 유연한 실행 제어.

---

## 4. 비기능 요구사항

| 항목 | 요구사항 |
|---|---|
| 실행 환경 | Windows, macOS, Linux (Node.js 24+ 필수) |
| 설정 보존 | 설정 정보는 글로벌(`~/.promptcraft/`) 또는 프로젝트 범주에 안전하게 저장 |
| 설치 방식 | pnpm 환경 글로벌 설치 또는 로컬 클론 |
| LLM 의존성 | **코어 생성 로직(스캔/Q&A/빌드)은 LLM API 없이 100% 로컬 동작 보장** |
| 타입 안정성 | 전체 프로젝트는 TypeScript로 개발되어 유지보수성을 극대화 |

---

## 5. 기술 스택

| 영역 | 기술/도구 |
|---|---|
| CLI 애플리케이션 | TypeScript, Node.js, Commander.js, Ink (React UI), @inquirer/prompts |
| 파일 스캔 / 엔진 | TypeScript (fs, path 조작, AST 구조 파악) |
| 데이터베이스 | SQLite (better-sqlite3) |
| 템플릿 엔진 | Handlebars (프롬프트 템플릿 조립) |
| 린트/포맷 가이드 | Biome |

---

## 6. 개발 타임라인

### 4개월 기준

| 기간 | 마일스톤 | 주요 작업 |
|---|---|---|
| 1개월차 | 인프라/설계 | 요구사항 모델링, 스캔 로직 구성, CLI 스켈레톤, TS 마이그레이션 |
| 2개월차 | 코어 엔진 MVP | 파일 스캐너, 인터랙티브 Q&A(Ink), 설정 관리(Config), 프롬프트 빌더 |
| 3개월차 | 기능/성능 고도화 | 프롬프트 히스토리 구현, 템플릿 커스텀 기능, 스캔 성능 튜닝 |
| 4개월차 | 퀄리티 폴리싱 | 버그 픽스, 에지 케이스 테스트, 릴리스 및 매뉴얼 작성 |

---

## 7. MVP 범위 및 변경점(PRD 2.2 기준)

### 포함 및 주요 마이그레이션 사항
- **TypeScript 전면 전환**: 기존 JS 기반에서 엄격한 TS 환경 설계 및 적용
- **CLI 인터랙티브 UI**: `Ink` 적용으로 직관적이고 시각적인 터미널 인터페이스 구축
- **Config 시스템 도입**: `config` 명령어 기반 전역/프로젝트 스코프 분할 관리
- 로컬 파일 스캔 및 분기 트리(JSON) 기반 Q&A 프로세스 보강
- `--output`, `--template` 등 다양한 활용 시나리오를 대응하는 빌드 CLI 옵션

### 의도적 배제
- 개발 극초기 단계의 LLM을 통한 자연어 후처리 로직 (우선 완전 자체 생성에 집중)

---

## 8. 성공 지표

| 지표 | 측정 방법 |
|---|---|
| 프롬프트 생성 소요 시간 | 수동 작성 대비 70% 이상 단축 (Q&A 완료 시점부터 복사까지) |
| 구조화 정확성 검증 | 생성된 Markdown 템플릿 섹션 파싱의 무결성 검증 |
| 구동 안정성 & 타입 | Typescript 컴파일 에러 Zero / Build 단계에서 Error Zero 유지 |
